{
  "name": "green-leaf-school",
  "version": "1.0.0",
  "private": true,
  "description": "Green Leaf International School & College — Full-Stack Website",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:admin\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev:admin": "cd server/admin && npm run dev",
    "build": "npm run build:client && npm run build:admin",
    "build:client": "cd client && npm run build",
    "build:admin": "cd server/admin && npm run build",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd admin && npm install",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
